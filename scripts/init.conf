# This file belongs in /data/pi

# The UID of the user in Host
USERID=1000

# Maximum memory for the container (m)
MEMORY=256

# Services
nginx_enabled=false
php_fpm_enabled=false
mariadb_enabled=false
postgresql_enabled=false
gitea_enabled=false