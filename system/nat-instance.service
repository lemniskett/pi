[Unit]
Description=NAT instance
Wants=network-online.target
After=network-online.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c "sysctl -w net.ipv4.ip_forward=1 && iptables -t nat -A POSTROUTING -o wlan0 -j MASQUERADE"

[Install]
WantedBy=multi-user.target